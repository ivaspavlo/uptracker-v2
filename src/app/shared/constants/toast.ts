
export enum ToastTypes {
  info = 'info',
  error = 'error',
  success = 'success'
}

export enum ToastPosition {
  top = 'top',
  bottom = 'bottom',
  start = 'start',
  center = 'center',
  end = 'end',
  left = 'left',
  right = 'right'
}
